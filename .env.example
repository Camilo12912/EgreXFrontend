# Configuración de API para Frontend (CRA)

# URL del backend (Ejemplo: https://tu-backend.up.railway.app o http://localhost:8080)
# Si se deja vacío, se usará '/api' como relativo (útil para Nginx en Docker)
REACT_APP_API_URL=
